var usuarios = [];


function ejercicio_10(nombre, Apellido, FechaNac, telefono, correo){

    var persona = new Object();

    persona.Name=nombre,
    persona.LastName=Apellido,
    persona.Date=FechaNac,
    persona.tel=telefono,
    persona.email=correo

    usuarios.push(persona);
    console.log(usuarios.persona.Name);
    console.log(usuarios.persona.LastName);
    console.log(usuarios.persona.Date);
    console.log(usuarios.persona.tel);
    console.log(usuarios.persona.email);
}

